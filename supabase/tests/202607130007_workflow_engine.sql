-- Disposable SQL scenarios: anonymous cannot read workflow instances; scoped user cannot cross tenant/organization/facility; published versions reject update/delete; confidential comments require workflows.view_confidential_comments; ordinary user cannot forge system workflow events.
-- Migration 202607130008 scenarios: reject wrong definition/version/instance child scope, unsafe condition/action JSON, duplicate initial state, invalid transition, missing reason/permission, cross-scope assignee, and direct event insertion. Verify publish only accepts approved valid version and current version is its own published version.
