import {describe,expect,it} from 'vitest'; import {createDraftWorkflowVersion,escalationEligible,slaOverdue,transitionAllowed,type WorkflowDefinition,type WorkflowInstance,type WorkflowVersion} from '@healthcare/workflow';
const scope={tenantId:'t',organizationId:'o',facilityId:'f'}; const version:WorkflowVersion={...scope,id:'v',definitionId:'d',version:1,status:'published',states:[],transitions:[{key:'submit',from:'draft',to:'submitted',label:{en:'Submit'},requiredPermission:'workflows.transition',allowedRoles:[],requiredFields:['title'],conditions:[],requiresReason:true,requiresComment:false,requiresAttachment:false,requiresApproval:false,actions:[],auditAction:'submit',notificationEvents:[]}],createdAt:'',createdBy:'u'}; const definition:WorkflowDefinition={...scope,id:'d',moduleKey:'medication_errors',name:{en:'Workflow'},tags:[],ownerId:'u',status:'published'}; const instance:WorkflowInstance={...scope,id:'i',workflowVersionId:'v',sourceType:'test',sourceId:'s',state:'draft',locked:false,revision:1,createdAt:'',createdBy:'u'};
describe('shared workflow engine',()=>{it('validates transitions, permissions, fields, and reasons',()=>{expect(()=>transitionAllowed(version,instance,'submitted',[],{title:'x'},'reason')).toThrow();expect(()=>transitionAllowed(version,instance,'submitted',['workflows.transition'],{},'reason')).toThrow();expect(()=>transitionAllowed(version,instance,'submitted',['workflows.transition'],{title:'x'})).toThrow();expect(transitionAllowed(version,instance,'submitted',['workflows.transition'],{title:'x'},'reason').key).toBe('submit');});it('creates immutable published draft versions and calculates SLA escalation',()=>{expect(createDraftWorkflowVersion(definition,version,'u').status).toBe('draft');expect(slaOverdue('2000-01-01T00:00:00Z')).toBe(true);expect(escalationEligible('2000-01-01T00:00:00Z',5)).toBe(true);});});
