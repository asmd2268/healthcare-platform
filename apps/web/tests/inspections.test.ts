import {describe,expect,it} from 'vitest'; import {demoSession,demoVersion,demoTemplate} from '@/features/inspections/repository'; import {createDraftVersion,generateFindings,requiredResponsesMissing,scoreInspection} from '@healthcare/inspections';
describe('inspection foundation',()=>{it('scores weighted answers and critical failure',()=>{const r=[{questionId:'question-1',optionId:'noncompliant',evidenceIds:[]}];const s=scoreInspection(demoVersion,r,80);expect(s.percentage).toBe(0);expect(s.criticalFailure).toBe(true);expect(s.passed).toBe(false)});it('excludes not applicable and validates required answers',()=>{expect(requiredResponsesMissing(demoVersion,[])).toEqual(['question-1']);expect(scoreInspection(demoVersion,[{questionId:'question-1',notApplicable:true,evidenceIds:[]}],80).rawScore).toBe(0)});it('creates a draft version and finding',()=>{expect(createDraftVersion(demoTemplate,demoVersion,'x').status).toBe('draft');expect(generateFindings({...demoSession,responses:[{questionId:'question-1',optionId:'noncompliant',evidenceIds:[]}]},demoVersion)).toHaveLength(1)})});
