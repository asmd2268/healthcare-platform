import {describe,expect,it} from 'vitest'; import {createDraftFormVersion,hasPlatformPermission,validatePermanentDeletion} from '@healthcare/platform-administration';
describe('platform administration contracts',()=>{it('enforces scope and permission',()=>{expect(hasPlatformPermission({key:'facility_administrator',scope:{level:'facility',organizationId:'o',facilityId:'f'},permissions:['platform.manage_forms']},'platform.manage_forms',{level:'facility',organizationId:'o',facilityId:'f'})).toBe(true);expect(hasPlatformPermission({key:'facility_administrator',scope:{level:'facility',organizationId:'o',facilityId:'f'},permissions:['platform.manage_forms']},'platform.manage_forms',{level:'facility',organizationId:'o',facilityId:'other'})).toBe(false)});it('protects published forms with deep draft copy',()=>{const form={id:'f',tenantId:'t',organizationId:'o',name:{en:'Form'},status:'published' as const,currentVersionId:'v'};const version={id:'v',formId:'f',version:1,status:'published' as const,createdAt:'',fields:[{id:'x',label:{en:'A'},type:'text' as const,required:true}]};const draft=createDraftFormVersion(form,version);draft.fields[0].label.en='B';expect(version.fields[0].label.en).toBe('A')});it('requires permanent deletion safeguards',()=>{expect(()=>validatePermanentDeletion({recordId:'x',reason:'',typedConfirmation:'x',dependenciesChecked:true,backupConfirmed:true,reauthenticated:true,secondApprovalRequired:false,protectedRecord:false})).toThrow();expect(()=>validatePermanentDeletion({recordId:'x',reason:'ok',typedConfirmation:'x',dependenciesChecked:true,backupConfirmed:true,reauthenticated:true,secondApprovalRequired:false,protectedRecord:true})).toThrow()})});
