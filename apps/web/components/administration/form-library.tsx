'use client'; import {useEffect,useState} from 'react'; import {useTranslations} from 'next-intl'; import {administrationRepository,type DemoForm} from '@/features/administration/repository';
export function FormLibrary(){const t=useTranslations('administration');const [forms,setForms]=useState<DemoForm[]>([]);const [q,setQ]=useState('');useEffect(()=>{administrationRepository.forms().then(setForms)},[]);return <section className="page"><h1>{t('library')}</h1><p>{t('placeholder')}</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder={t('search')}/>{forms.filter(f=>`${f.nameAr} ${f.nameEn}`.includes(q)).map(f=><article className="ui-card" key={f.id}><strong>{f.nameAr} / {f.nameEn}</strong><p>{f.module} · v{f.version} · {f.status}</p><button disabled={f.status==='published'}>{t('editDraft')}</button><button>{t('preview')}</button></article>)}</section>}
