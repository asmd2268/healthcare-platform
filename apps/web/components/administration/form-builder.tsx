'use client'; import {useState} from 'react'; import {useTranslations} from 'next-intl';
type Field={id:number;type:string;ar:string;en:string;required:boolean}; export function FormBuilder(){const t=useTranslations('administration');const [fields,setFields]=useState<Field[]>([]);const add=()=>setFields([...fields,{id:Date.now(),type:'text',ar:'',en:'',required:false}]);return <section className="page"><h1>{t('builder')}</h1><p>{t('placeholder')}</p><input placeholder={t('arabicName')}/><input placeholder={t('englishName')}/><button onClick={add}>{t('addField')}</button>{fields.map((f,i)=><div className="ui-card" key={f.id}><select value={f.type} onChange={e=>setFields(fields.map(x=>x.id===f.id?{...x,type:e.target.value}:x))}><option>text</option><option>number</option><option>date</option><option>dropdown</option><option>file</option><option>signature</option></select><input placeholder={t('arabicLabel')} value={f.ar}/><input placeholder={t('englishLabel')} value={f.en}/><button onClick={()=>setFields(fields.filter(x=>x.id!==f.id))}>{t('remove')}</button><button disabled={i===0} onClick={()=>setFields(fields.map((x,n)=>n===i?fields[i-1]:n===i-1?f:x))}>{t('up')}</button></div>)}<button>{t('saveDraft')}</button></section>}
