'use client';
import {useState} from 'react'; import {useTranslations} from 'next-intl'; import {Button, Card, Textarea} from '@healthcare/ui'; import {demoSession,demoVersion} from '@/features/inspections/repository'; import {requiredResponsesMissing} from '@healthcare/inspections';
export function ExecutionFlow(){const t=useTranslations('inspections.execution');const [draft,setDraft]=useState(demoSession);const [saved,setSaved]=useState(false);const q=demoVersion.sections[0].questions[0]; const missing=requiredResponsesMissing(demoVersion,draft.responses); return <section className="page"><h1>{t('title')}</h1><p>{t('placeholder')}</p><Card><h2>{demoVersion.sections[0].title.en}</h2><p>{q.text.en}</p>{q.options.map(o=><Button key={o.id} onClick={()=>setDraft({...draft,responses:[{questionId:q.id,optionId:o.id,evidenceIds:[]}]})}>{o.label.en}</Button>)}<Textarea placeholder={t('notes')} /><Button onClick={()=>setSaved(true)}>{t('save')}</Button>{saved&&<p>{t('saved')}</p>}{missing.length>0&&<p>{t('required')}</p>}</Card></section>}
